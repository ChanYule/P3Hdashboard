# Copy to .env and set real local values when SMTP delivery is enabled.
SECRET_KEY=replace-with-a-secure-local-secret
MAX_UPLOAD_MB=25
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true
